import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import Car from './Car';

ReactDOM.render(
  <React.StrictMode>
    <App />
    <Car />
  </React.StrictMode>,
  document.getElementById('root')
);

